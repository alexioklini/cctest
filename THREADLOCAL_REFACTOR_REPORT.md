# Thread-Local Refactor Report (Tier G) — living source of truth

Companion to `THREADLOCAL_REFACTOR_PLAN.md` (the plan). **This file records what is DONE.**
HTML view (`THREADLOCAL_REFACTOR_REPORT.html`) is generated by `refactor_publish.sh` — never hand-edit it.

Goal: every request-context attribute migrated off raw `_thread_local` onto a typed
`RequestContext` (contextvars-backed) accessed through ONE module; all enter/exit sites use a
single context-manager (no scattered `=None` teardown); a new concurrency/bleed test passes;
`./refactor_gate.sh` green after every step.

---

## Status board

| Phase | What | Status |
|-------|------|--------|
| 0 | Build the gate (bleed test + tlgrep mode) + inventory | ✅ done |
| 1 | RequestContext + ContextVar + compat shim (no call-site changes) | ⬜ |
| 2 | Migrate enter/exit sites to `request_context(...)` ctx-manager | ⬜ |
| 3 | Migrate reads to typed accessors (kill raw getattr) | ⬜ |
| 4 | Remove the shim | ⬜ |
| 5 | Final source-validation | ⬜ |

**RESUME POINT:** Phase 1 — introduce RequestContext + ContextVar + compat shim (no call-site changes).

---

## Module-home decision

**Extend `engine/context.py`** (not a new `engine/reqctx.py`). Rationale: `engine/context.py` is
already the cycle-free home of `_thread_local` (imports only stdlib, never `import brain`), and is
already the import target for all three spellings (`engine._thread_local`, `brain._thread_local`,
`from engine.context import _thread_local`). Putting `RequestContext` + the ContextVar + the shim
here keeps the single-instance-identity invariant trivially true and adds no new import edges.

---

## Decision log

- **2026-05-23** — Module home = `engine/context.py` (above).
- **2026-05-23** — Dynamic-key escape hatch: `tool_exec._get_artifact_session_folder` writes
  `_artifact_folder_<session_id>` keys via `setattr(_thread_local, ...)`. The dataclass can't
  enumerate these, so the shim + RequestContext keep a `_dynamic: dict` bucket for any attribute not
  declared as a typed field. Reads/writes of unknown attrs route there. Listed in the inventory as
  the `_artifact_folder_*` (dynamic) row.
- **2026-05-23** — Inventory measured 37 typed attrs (see below). Five were NOT in the plan's
  indicative list but ARE genuine request-context attrs found by the getattr scan:
  `_tool_results_tokens`, `audit_source`, `current_model`, `current_trace_span`,
  `workflow_allowed_tools`. The plan explicitly states the measured inventory is authoritative;
  these are recorded here (not a feature-scope change — all are per-request execution state).
  Plan-listed `in_worker_subagent` has ZERO refs in live code (removed by SDK Phase 5) — dropped.

---

## Attribute inventory (AUTHORITATIVE — fixed scope, statuses only flip ⬜→🔄→✅)

Ref counts measured 2026-05-23 from `brain.py engine/ handlers/ server_lib/ server.py server_daemons.py`
via combined dotted-access + `getattr` + `setattr` scan. Counts are indicative of blast radius.

| Attribute | Owning subsystem | getattr default | Status |
|-----------|------------------|-----------------|--------|
| `current_agent` | core request | `None` | ⬜ |
| `current_user_id` | core request | `""` / `None` | ⬜ |
| `current_session_id` | core request | `""` / `None` | ⬜ |
| `session_id` | core request | `""` / `None` | ⬜ |
| `current_team_ids` | core request | `[]` | ⬜ |
| `memory_store` | core request | `None` | ⬜ |
| `mcp_manager` | core request | `None` | ⬜ |
| `project` | core request | `""` / `None` | ⬜ |
| `delegate_agent_id` | delegation | `None` | ⬜ |
| `current_worker_id` | delegation | `None` | ⬜ |
| `_current_model` | model select | `None` | ⬜ |
| `current_model` | model select | `None` | ⬜ |
| `research_mode_override` | project mode | `None` | ⬜ |
| `event_callback` | chat stream | `None` | ⬜ |
| `note_context` | chat | `None` | ⬜ |
| `plan_mode` | chat | `False` | ⬜ |
| `caveman_system` | caveman | `0` | ⬜ |
| `caveman_chat` | caveman | `0` | ⬜ |
| `attachment_image_model` | attachments | `''` | ⬜ |
| `attachments` | attachments | (dotted) | ⬜ |
| `_gdpr_mapping_id` | gdpr | `""` | ⬜ |
| `_gdpr_anonymising` | gdpr | `False` | ⬜ |
| `_gdpr_after_file_write_cb` | gdpr | `None` | ⬜ |
| `trace_id` | tracing | `None` | ⬜ |
| `current_trace_span` | tracing | `None` | ⬜ |
| `audit_source` | audit | `'chat'` | ⬜ |
| `tool_use_id` | tool exec | `None` | ⬜ |
| `tool_round` | tool exec | (dotted) | ⬜ |
| `_tool_results_tokens` | tool exec | `0` | ⬜ |
| `_discovered_tools` | tool exec | `set()` | ⬜ |
| `execution_overrides` | exec | (dotted) | ⬜ |
| `_intent_action_recovery_count` | exec | (dotted) | ⬜ |
| `_guided_tasks_for_msg` | exec | (dotted) | ⬜ |
| `workflow_run_id` | workflow | (dotted) | ⬜ |
| `workflow_execution_id` | workflow | `None` | ⬜ |
| `workflow_default_model` | workflow | `""` | ⬜ |
| `workflow_agent_id` | workflow | `""` | ⬜ |
| `workflow_allowed_tools` | workflow | `None` | ⬜ |
| `_artifact_folder_*` (dynamic) | tool exec | `None` | ⬜ |

**37 typed attributes + 1 dynamic pattern.** This is the complete, fixed scope. The run only flips
statuses; it never appends. A genuinely new attribute = scope change to flag to the user.

---

## Import spellings (all must keep resolving until Phase 4)

| Spelling | Live ref count (2026-05-23) |
|----------|------------------------------|
| `engine._thread_local.x` (incl. `import brain as engine`) | dominant — handlers/chat.py 60, server.py 11, handlers/admin_config.py 6, etc. |
| `brain._thread_local` | 10 |
| `from engine.context import _thread_local` | 9 files (prompt_build, mempalace_glue, tool_exec, tools/*) |

---

## Per-step record

(appended one block per gated commit)

### Phase 0 — gate + inventory ✅ (commit: this)
- Inventory measured + recorded above (37 attrs + 1 dynamic pattern). Authoritative scope fixed.
- `tests/test_request_context_isolation.py` written — 3 tests: `test_no_bleed_under_overlap`
  (24 tasks / 4-thread pool, barrier-synced + staggered sleeps, reads back only own ctx at every
  checkpoint), `test_teardown_leaves_no_residue` (single-worker reuse sees defaults after teardown),
  `test_negative_control_skipped_teardown_bleeds` (proves the bleed IS observable when teardown is
  skipped — so the positive tests aren't vacuously green). **All 3 pass on TODAY's raw
  `threading.local()` design** — correct green baseline before any change.
- `refactor_gate.sh tlgrep <attr>` mode added (gate component 2): proves a migrated attr has no raw
  `_thread_local.<attr>` / `getattr`/`setattr` access in live code outside `engine/context.py`.
  Verified it FAILS on an unmigrated attr (`current_agent`) and is OK on a never-used name.
- Bleed test wired into the gate as **Gate 5b** (explicit headline-criterion callout, fails the gate
  independently of the broad `unittest discover`).
- Full `./refactor_gate.sh` GREEN: 18/18 imports, PII parity OK, no new unittest fails beyond the 3
  known spaCy ones, Gate 5b OK.
