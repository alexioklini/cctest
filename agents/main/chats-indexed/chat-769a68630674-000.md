---
name: "what do you know about remote nodes (part 1/1)"
type: chat_transcript
description: "Explained remote node capabilities for executing commands."
session_id: 769a68630674
agent: main
chunk_index: 0
total_chunks: 1
created_at: "2026-04-06T14:20:57.665276"
last_recalled: 2026-04-09
related:
  - file: chats-indexed/chat-4a6d72676ba1-000.md
    type: contradicts
  - file: chats-indexed/chat-e44642ceda4a-000.md
    type: same_topic
  - file: chats-indexed/chat-e44642ceda4a-003.md
    type: same_topic
  - file: chats-indexed/chat-e44642ceda4a-002.md
    type: references
  - file: chats-indexed/chat-e44642ceda4a-001.md
    type: same_topic
  - file: chats-indexed/chat-024258860a16-001.md
    type: extends
---

### Example Workflow:

**user**: what do you know about remote nodes

**assistant**: I can interact with remote nodes to execute commands, read/write files, and perform other operations. Remote nodes are registered in the system and can be accessed using the `node` parameter in tools like `execute_command`, `read_file`, `write_file`, and `list_directory`.

### Key Points About Remote Nodes:
1. **Node Identification**:
   - You can specify a remote node by its name (e.g., `node="my-server"`).
   - You can also target nodes by tag (e.g., `node="tag:compute"`).

2. **Capabilities**:
   - Remote nodes can execute shell commands, read/write files, and list directories.
   - They can be used for distributed tasks or accessing resources on other machines.

3. **Listing Nodes**:
   - Use the `list_nodes` tool to see all registered remote nodes, their status, OS, tags, and resource usage.

4. **Use Cases**:
   - Offloading tasks to specialized hardware (e.g., GPU nodes).
   - Accessing files or services on remote machines.
   - Running commands on multiple nodes in parallel.

Would you like me to list the currently registered remote nodes or perform an operation on a specific node?

**user**: do you know to install remote nodes

**assistant**: Yes! Remote nodes can be installed and registered to extend the capabilities of the Brain Agent system. Here’s how it works:

### How to Install Remote Nodes:
1. **Prerequisites**:
   - The remote machine must have network access to the main Brain Agent instance.
   - The remote machine must have the necessary tools and dependencies installed (e.g., Python, required libraries).

2. **Installation Process**:
   - The remote node software (agent) must be installed on the target machine.
   - The node must be configured to connect to the main Brain Agent instance (e.g., via a registration token or API key).

3. **Registration**:
   - Once installed, the node registers itself with the main Brain Agent system.
   - The node can be tagged (e.g., `tag:compute`, `tag:gpu`) for easy targeting.

4. **Verification**:
   - Use the `list_nodes` tool to verify that the node is registered and available.

### Example Workflow:
1. **Install the Remote Agent**:
   - Deploy the Brain Agent software on the remote machine.
   - Configure it to connect to the main instance.

2. **Register the Node**:
   - The node will automatically register itself or require manual registration via a command or API call.

3. **Use the Node**:
   - Once registered, you can target the node in tools like `execute_command` or `read_file` using the `node` parameter.

Would you like me to check the current list of registered nodes or guide you through the installation process for a specific type of node?