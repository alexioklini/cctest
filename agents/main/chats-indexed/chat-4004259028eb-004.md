---
name: ""summary please  [User attached files — saved to disk. Use re (part 5/9)""
type: chat_transcript
description: ""There's an issue with the selected model (devstral-small-latest). It may not exi""
session_id: 4004259028eb
agent: main
chunk_index: 4
total_chunks: 9
created_at: "2026-04-07T22:03:56.179581"
---

#### Requirements

o disk. IMPORTANT: Use the read_document tool (NOT read_file) to read these — read_document handles PDF, DOCX, XLSX, PPTX and other document formats:]
  - /tmp/brain-attachments/4004259028eb/INFERENCER_DISTRIBUTED_GUIDE.docx

**assistant**: ### Summary of the Inferencer Distributed Compute User Guide

#### Overview
- **Inferencer Pro** is an MLX-native inference server for macOS that supports distributed computing across two Apple Silicon Macs.
- **Key Features:**
  - Relay vertical architecture: One GPU is active at a time, passing layers back and forth.
  - Supports pairs of machines (maximum 2 machines).
  - Asymmetric RAM is supported.
  - Greedy layer distribution: Automatic, no manual configuration needed.
  - Average speed: Each machine runs at its own max speed; the result is the average of both.

#### Requirements
- **Hardware:**
  - 2 Apple Silicon Macs (Mac Studio, MacBook Pro, Mac Mini — any combination).
  - Both on the same local network (Ethernet recommended, WiFi viable).
  - Thunderbolt 5 direct connection is supported but has known stability issues.
- **Software:**
  - Inferencer Pro v1.10.4+ (macOS App Store).
  - macOS 2

*(Cancelled)*

**user**: please describe

**tool**: {"path": "/tmp/brain-attachments/4004259028eb/INFERENCER_DISTRIBUTED_GUIDE.docx", "format": "docx", "content": "Inferencer Pro\n\nDistributed Compute User Guide\n\nPractical guide for setting up and running Inferencer Pro in distributed mode across two Apple Silicon Macs.\n\nBased on real-world testing: M3 Ultra 512 GB + M3 Ultra 256 GB — Inferencer v1.10.4 — macOS 26.3\n\nSophie — The Monocle Bear\n\nthemonoclebear.com\n\nLast updated: March 4, 2026\n\n# 1.

Overview\n\nInferencer Pro is an MLX-native inference server for macOS.

Its distributed compute feature allows two Macs to pool their unified memory to run models that exceed a single machine's capacity.\n\nKey characteristics:\n\nRelay vertical architecture — one GPU is active at a time, passing layers back and forth.

This doubles available RAM, not speed.\n\nPairs only — maximum 2 machines (>2 support is on the roadmap).\n\nAsymmetric RAM supported — tested with 512 GB + 128 GB, 512 GB + 256 GB.\n\nGreedy layer distribution — automatic, no manual configuration needed.\n\nAverage speed — each machine runs at its own max speed; the result is the average of both, not degradation to the slowest.\n\n# 2.

Requirements\n\n## Hardware\n\n2 Apple Silicon Macs (Mac Studio, MacBook Pro, Mac Mini — any combination)\n\nBoth on the same local network (Ethernet recommended, WiFi viable)\n\nThunderbolt 5 direct connection is supported but has known stability issues (GitHub #161)\n\n## Software\n\nInferencer Pro v1.10.4+ (macOS App Store)\n\nmacOS 26.x recommended\n\nThe same model must be downloaded/accessible on both machines\n\n## Network\n\n# 3.

Architecture\n\nCLIENTS (unlimited)                    COMPUTE (pair)\n\n───────────────────                    ──────────────\n\nn8n, Cline, VS Code    ──┐\n\nOpenWebUI, curl         ──┤──→  Machine A (Server)  ←→  Machine B (Compute Node)\n\nClaude Code (future)    ──┘     Port 54321               Port 8081\n\nAPIs ON                   Connect → A\n\nOrchestrates split        Provides RAM + compute\n\n## Roles\n\nCritical: Machine B connects to Machine A, not the other way around.

If reversed, A becomes a passive client and doesn't use its own RAM.\n\n# 4.

Setup — Step by Step\n\n## Step 1 — Configure Machine B (Compute Node)\n\nOpen Inferencer Pro on Machine B.

Go to Server > Serve:\n\nClick Start Server.

Note the hostname and port (e.g., ultra-256a.local:8081).\n\n## Step 2 — Configure Machine A (Server)\n\nOpen Inferencer Pro on Machine A.

Go to Server > Serve:\n\nClick Start Server.\n\nThen in Settings (gear icon):\n\nIn Inference Settings > Generation:\n\n## Step 3 — Connect B to A\n\nOn Machine B, go to Server > Connect.\n\nMachine A should appear via auto-discovery: ultra-512.local - Inferencer Server (Unencrypted)\n\nIf not, enter the IP/hostname manually: ultra-512.local:54321\n\nClick Connect\n\nEnable Auto-connect for automatic reconnection\n\n## Step 4 — Verify Connection\n\nOn Machine B's Connect panel, you should see Machine A's details: hostname, Inferencer version, device info, memory usage, and Server Models — the list of models available on Machine A.\n\nExpected behavior: Machine B's UI now shows Machine A's model list.

This is normal — B delegates its interface to the server.\n\n# 5.

Loading a Distributed Model\n\n## Prerequisites\n\nThe model MUST be present on BOTH machines.

Inferencer will only distribute a model if both nodes have it in their local folders.

Without this, the model loads entirely on one machine.\n\nDownload the same model on both machines via Models > Hugging Face, or copy the model files to both machines' local folders (Models > Local Folders).

The model ID must match exactly.\n\n## Loading Procedure\n\nOn Machine B (which shows Machine A's interface), select the model\n\nToggle Distributed Compute ON in the chat bar (two-node icon at the top)\n\nThe model loads and distributes across both machines\n\n⚠️ The Distributed Compute toggle is per-conversation.

Enabling it in Server > Serve is necessary but not sufficient — you must also toggle it in each chat session.\n\n## What Triggers a Real Split?\n\nInferencer only splits the model if it exceeds the primary machine's available RAM.

If the model fits on Machine A alone, it stays there entirely — even with B connected and distributed enabled.\n\nTested examples (512 GB + 256 GB pair):\n\n## Memory Budget\n\nAvailable RAM ≈ Total RAM − system overhead (~30 GB per machine).\n\nFor a 512 + 256 pair: ~480 + ~240 = ~720 GB usable.

The model weights + KV cache + activations must fit within this.\n\n# 6.

API Access\n\n## Endpoints\n\n## Model Prefixes\n\nWhen distributed is active, /v1/models returns three types:\n\nImportant: Without the <distributed> prefix, the system may eject ~50% of the model and consolidate on one machine.

Always use the prefix for models larger than a single machine's capacity.\n\n## Example API Calls\n\n# List models\n\ncurl http://192.168.86.29:54321/v1/models | jq .\n\n# Chat completion (distributed)\n\ncurl -X POST http://192.168.86.29:54321/v1/chat/completions \\\n\n-H \"Content-Type: application/json\" \\\n\n-H \"Authorization: Bearer x\" \\\n\n-d '{\n\n\"model\": \"<distributed>/Kimi-K2.5-MLX-4.2bit\",\n\n\"messages\": [\n\n{\"role\": \"system\", \"content\": \"You are a helpful assistant.\"},\n\n{\"role\": \"user\", \"content\": \"Explain GDPR in 3 sentences.\"}\n\n],\n\n\"temperature\": 0,\n\n\"max_tokens\": 200\n\n}'\n\n## Authentication\n\nAPI key is optional on local network — use Bearer x as placeholder\n\nNo rate limiting documented\n\nFor internet access: enable Internet Connections + configure IP allow/block list in Security Settings\n\n## Client Compatibility (v1.10.4)\n\n# 7.

Prompt Caching\n\nInferencer supports prefix-matching prompt caching.

If consecutive requests share the same system prompt, only the first request processes it fully — subsequent requests hit the cache for a 99x speedup.\n\n## Performance Impact\n\nRequest 1:  ~90 sec  (full system prompt processing)\n\nRequest 2:  <1 sec   (cache hit — system prompt already processed)\n\nRequest 3+: <1 sec   each\n\nBatch of 33 requests (same system prompt):\n\nWithout cache: 33 × 90 sec = ~49 min\n\nWith cache:    90 + 32 × 1  = ~2 min\n\nSavings:       ~47 minutes per cycle\n\n## Configuration\n\n## What Invalidates the Cache\n\n# 8.

Cache Quantization (Precision)\n\nCache quantization reduces the memory consumed by conversation context by up to 3.55x.\n\nLocation: Inference Settings > Model > Precision\n\nAvailable levels: 16-bit (default), 9-bit, 8.5-bit, 6.5-bit, 5.5-bit, 4.5-bit, 3.5-bit.\n\n## Measured Data (~2,600 tokens)\n\nRule of thumb: For DeepSeek and Kimi models, always enable 4.5-bit cache quantization.