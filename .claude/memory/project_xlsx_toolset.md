---
name: project_xlsx_toolset
description: "v9.262.0 deterministisches XLSX-Toolset (xlsx_inspect/query/create/edit) SHIPPED — Modell liefert nur SQL/JSON-Spec, Daten laufen serverseitig; live mit mistral-small am Marktorder-Fall verifiziert"
metadata: 
  node_type: memory
  type: project
  originSessionId: 21d1fa5b-6c60-4bbe-8204-cb1b4ee1b0da
---

v9.262.0 (2026-07-02, Commit 2b99dfd1): **Deterministisches XLSX-Toolset SHIPPED** — 4 Tools in `engine/tools/xlsx_tools.py` (documents-Gruppe), Anlass Chats 2cb94154 (mistral-medium: 6 Skript-Iterationen, nur CSV) vs 98cceac2 (deepseek: 1 Wurf). Prinzip wie docx/html: Modell liefert nur Intent (SQL-SELECT / kleine JSON-Spec), Server bewegt Daten — Massendaten nie durchs Modell.

- **xlsx_inspect**: Profil + Join-Key-Kandidaten (Wert-Overlap ≥50%) + copy-paste `Tables for xlsx_query:`-Block mit exakten sanitized Bezeichnern (der 7B-Reliability-Hebel).
- **xlsx_query**: SELECT-only In-Memory-SQLite, 3-schichtig (Prefix-Check, sqlite3-Authorizer SELECT/READ/FUNCTION/RECURSIVE, `query_only=ON`); SQL-Fehler echoen Schema (Selbstkorrektur); `paths[]` = Multi-File-Session mit Datei-Stem-Prefix; `out=` → volle CSV als Artifact; Caps 50 Zeilen Anzeige / 200k rows / 30MB.
- **xlsx_create**: Spec → openpyxl mit doc-style-Preset; `source:{file,sheet?,sql?}` = serverseitiger Fluss; `master_detail:{key,master,detail}` (Master getönt+bold key, Details mit outline_level 1 darunter); Charts (bar|line|pie) + Conditional (color_scale|data_bars|CellIs); inline-rows-Cap 5k Zellen steuert auf source.
- **xlsx_edit**: formaterhaltend (untouched cells bleiben; neue Zeilen erben Stil via `_style`-copy), Header in Zeile 1 angenommen, `formula:"=B{row}*C{row}"` pro Zeile gefüllt, keep_vba.
- v9.261.0-Trim jetzt `doc_convert._trim_placeholder_columns(header, rows, cell_fn)` — Byte-Stability-Pin in `tests/test_xlsx_tools.py` (VOR Refactor geschrieben; Mining-`.md` muss byte-stabil bleiben).
- `write_document`-.xlsx rendert durch `xlsx_tools.render_spec` (Markdown-Vertrag identisch, `_parse_md_table_rows` ersetzt `_write_md_table_to_sheet`); render_spec akzeptiert vorab geladenen `style`-Dict (kein Zyklus: xlsx_tools importiert file_tools nur lazy in Funktionen).
- Classifier-Steering: Spreadsheet-Arbeit → `files` NICHT `python` (sonst kriegt Modell code_exec und schreibt wieder pandas) + Beispiel.
- **LIVE VERIFIZIERT**: mistral-small (schwächer als das gescheiterte medium) löst den Marktorder-Fall in 1 Turn / 74s: xlsx_inspect→xlsx_create, 25 Master + 51 Detail exakt korrekt, null python_exec (Session 52b618dd7ba1).
- **v2 SHIPPED v9.263.0** (873d52f0, gleicher Tag): Multi-Table-Detection (Split auf ≥2 Leerzeilen + header-ish) + hierarchische merged Header ('Q1 / Umsatz', Merges via Zip-XML-Parse `_read_merges` — read_only-openpyxl exponiert merged_cells NICHT), `inspect deep=true` (Duplikate/3×IQR-Ausreißer/Orphan-Keys/Formel-Map), Pipeline-Handles (`save_as` → `result:name` in query/diff/create/edit, in-memory 8/Session), **xlsx_diff** (5. Tool, keyed Diff mit Alt→Neu + out=CSV), create-Extras (template-Fill an anchor/named_range, master_detail.subtotals, autofilter, choices-Dropdown, print-Setup), **UI-Grid-Vorschau** (`GET /v1/files/xlsx-grid` nutzt denselben Grid-Loader; xlsx-Klick im Datei-Baum → Grid-Tab, csv-Ansicht = Grid, Artifacts-Fullview zeigt Tabelle; net-globals 1831→1833). Live: mistral-small löst Bestandsvergleich via inspect→diff exakt. 45 Unit-Tests.
- **v3 SHIPPED v9.264.0** (a90822e4, gleicher Tag): xlsx_diff v2 (Composite-Keys, compare='formulas', out='diff.xlsx' = markiertes Workbook gelb/grün/rot + Kommentar 'vorher: X'), Pivot-Layout (`pivot:{rows,cols?,values,agg}` + Gesamt-Zeile), Charts v2 (area/scatter/stacked/secondary via axId=200+crosses=max), **Recalc** via LibreOffice-Roundtrip (`recalc:true`; `_find_soffice` config xlsx.soffice_path→Kandidaten→which) + .xls/.ods via mtime-Cache /tmp/brain-xlsx-convert, Groß-Dateien (>30MB mit sheet=, >50k-Fast-Path ohne per-Zelle-Styles + report_tool_progress), **editierbares UI-Grid** (Dblclick im Bottom-Panel → POST /v1/files/xlsx-cell mit 409-mtime-Check; Grid-Endpoint liefert row_nums+sheet_title; `_xlsxGridMount` stateful mit Sort/Suche/th-resize), JSON/XML-Baum (`_terminalPaintTree`) auch im rechten Panel, **Projekt-Struktur-Profile** (`_sync_project_xlsx_profiles` web-urls-Muster → pdir/xlsx-profiles/, mtime-Stamp im File-Head, Branch 1c mined ohne KG, Fingerprint um profile_folder erweitert), Scheduler-Rezepte in 04-recipes. GOTCHA gefunden: Artefakt-Panel-Gate prüfte nach dem Mount-Umbau noch `typeof _xlsxGridHtml` (gelöscht) → fiel still auf die Download-Karte; Playwright-Trace (Monkeypatch auf renderArtifactDocument/XlsxGrid) fand es. 52 xlsx-Tests inkl. echtem soffice-Recalc/ODS-Roundtrip.
- **v9.265.0** (e997be71): VBA-VIEWER — `doc_convert.list_vba_modules` (Zwilling des byte-stabilen `_extract_vba`) + `GET /v1/files/xlsm-vba` → ⚙-Modul-Reiter im Grid-Viewer (Bottom-Panel + Dateien-Tab), read-only + .bas-Export. **VBA-SCHREIBEN GEHT NICHT ohne Excel** (vbaProject.bin: MS-OVBA-Kompression + P-Code + Offsets; openpyxl nur keep_vba, LO-Roundtrip verstümmelt) — bewusst kein Save-Knopf. Plus: Zellen-Edit jetzt auch im Dateien-Tab des rechten Panels (editable:true; Anhänge bleiben read-only = Modell-Inputs).
- **v4 SHIPPED v9.266.0** (d64ead0c): (a) Write-only-STREAMING — render_spec löst Daten vorab auf, Blatt >WRITE_ONLY_ROWS=100k → ganzes Workbook openpyxl write_only (_render_spec_write_only: WriteOnlyCell-Header, Freeze/Widths VOR erstem append, =SUM-Totals, Progress je 50k; banded/Formats/Charts/Conditional entfallen mit note; master_detail/pivot+huge → Fehler); CREATE_MAX_SOURCE_ROWS=750k via _build_sqlite(max_rows=); live 150k Zeilen = 2,4s/3MB. (b) compare='formats' — _format_signature (lesbar: 'General · fett · füllung:FFFF00'), _sig_matrices (read_only-Pass), Ausrichtung via grid.row_nums; _diff_grids fmt_a/fmt_b-Overlay (rows_a jetzt key→(idx,row)); Zeilen-Match auf Wert-Key. (c) UNDO im Edit-Grid — st.undoStack in _xlsxGridMount, ↩-Button (Count, disabled bei leer), shared saveCell-Helper; Playwright-verifiziert Edit→Undo→Original+disabled. GOTCHA-Erinnerung: read_only-Worksheets haben KEIN freeze_panes-ATTRIBUT beim Lesen (nur Schreiben via regulärem Load prüfbar).
- Offen bleibt nur: VBA-Write (bräuchte Excel/COM auf Windows-Host).

Verwandt: [[project_empty_reply_and_wide_xlsx]] (der 9.261.0-Trim), [[feedback_commit_to_main]].
