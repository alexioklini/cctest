---
name: project_workflow_run_artifacts_and_binary_fix
description: v9.291.3 — Workflow-Lauf-Artefakte vollständig + Bild-Render + Purge-Guard; Diagnose an Lauf 739e97d0ca / Session dbf239aa
metadata: 
  node_type: memory
  type: project
  originSessionId: ae046055-a26f-44cc-a8fe-1fa024dda70a
---

v9.291.3: vier zusammenhängende Defekte am Workflow-Lauf (gemeldet an Lauf `739e97d0ca`, Chat-Session `dbf239aac399`). GOTCHA vorab: eine 8-Zeichen-ID im UI ist die **Session-ID** (chats.db `sessions.id`), NICHT die 10-Zeichen workflow_history.execution_id — Session trägt `workflow_run_id` → execution_id.

**(1) Fehlende Artefakte.** Der `agent_step`-Executor schrieb Dateien (ela_result.jpg, metadaten_ausweis.txt …) via `python_exec` (Pillow) in den Lauf-Artifact-Ordner. `tool_agent_step` (engine/tools/ask_tools.py) erfasst geschriebene Dateien aber NUR aus einer Whitelist `_write_tools = {write_file, edit_file, xlsx_*, render_diagram}` — python_exec/code_exec fehlen → Pfade landen weder in steps_json noch output_paths_json → `_workflow_run_paths_classified` (handlers/admin_workflows.py) findet sie per Regex nie. FIX (User-Wahl: Ordner scannen statt Whitelist erweitern): `_workflow_run_paths_classified` scannt jetzt den Lauf-Artifact-Ordner (dirname eines bekannten Output-Pfads, Basename endet auf `wf-<exec_id>` — Ordner heisst `<date>_wf-<exec_id>`, also SUFFIX-Match, nicht ganzer Basename!) und claimt jede Datei als output. Upload-Inputs liegen unter /tmp/brain-workflow-uploads/<exec_id>/ (NICHT im Artifact-Ordner) → bleiben via order-guard in claim() 'input'.

**(2) Input-Dateien im Panel.** Bereits ok: ask_user_for_file ∈ _WF_INPUT_TOOLS → references_out → Referenzen-Reiter. Durch (1) nicht fälschlich output.

**(3) 'failed' obwohl fertig.** Korrekt failed: LETZTER agent_step (Verify, model=glm-5.2 via Kilo) lief auf HTTP 429 [BYOK]-Rate-Limit; erster agent_step = mistral-small-latest (MODEL-Header, via mistral-direct) ok. Status bleibt ehrlich failed. `_wfSyncTranscriptMessages` (web/js/workflows.js) stellt bei terminal-failed/cancelled MIT Transcript einen Hinweisblock voran (429 erkannt → Rate-Limit-Text + Fehler; Teilergebnisse gültig).

**(4) JPG als Binärstrom statt <img>.** `_seed_artifacts_for_run` speicherte den ROHEN ext ('jpg') als `artifacts.type`. Der Content-Endpoint (`_handle_artifact_content`, handlers/admin_artifacts.py) gatet `is_binary = type in ('image','document','audio')` und gibt `type` verbatim zurück; Viewer (`renderArtifactContent`, panels_artifacts.js) schaltet auf `type==='image'`. Mit 'jpg' → als UTF-8 dekodiert + in hljs = Binärsalat. FIX: Seeding nutzt `brain._ARTIFACT_TYPE_MAP` (jpg/png→'image', md→'markdown', txt→'text', Fallback 'code') — DIESELBE Map wie der normale Datei-Write (brain.py ~14701). **GOTCHA: `brain` ist in admin_workflows.py NICHT als bare Global injiziert** (nur engine/client/_db_conn/sqlite3) — `from brain import _ARTIFACT_TYPE_MAP` lokal, wie die anderen Methoden (`from brain import _workflow_history_get`). Der Docstring-Claim 'brain als bare global' stimmt für diese Datei NICHT.

**(5) Beifang — Purge löschte Lauf-Sessions.** Startup-Empty-Purge (server.py ~3979) löschte 0-Nachrichten-Sessions >5min. Workflow-Lauf-Sessions tragen KEINE echten messages (nur synthetischer Transcript + workflow_run_id) → jeder Neustart löschte sie still aus der Sidebar. Die Laufzeit-Auto-Delete (db.py list_auto_deletable) schloss workflow_run_id schon aus — jetzt auch der Startup-Purge (`COALESCE(workflow_run_id,'')=''`). KEIN Datenverlust (workflow_history + Disk bleiben; Session re-mintet bei Öffnen), nur Sidebar-Eintrag/Seed weg.

**(6) v9.291.4 — Upload fehlt im Anhänge-Reiter.** Der Eingabe-Upload hängt als `_files` (Unterstrich!) am synthetischen USER-Turn (von `_wfSyncTranscriptMessages`). `collectChatAttachments` (panels_artifacts.js) füllt den Anhänge-Reiter, las aber nur `files`/`images`/content-blocks (OHNE Unterstrich) → Upload nie im Reiter (nur als 📎 im Fluss + Text-Karte in Referenzen). FIX: collectChatAttachments liest jetzt `msg._files` auf `_wfSynthetic`-USER-Turns (schon auf user/human gescoped = Inputs) → pfadbasierte Karte. DOWNLOAD-GOTCHA: Uploads unter /tmp/brain-workflow-uploads/<exec>/ → allgemeiner `/v1/files/download`-Validator LEHNT /tmp AB. Muss über gescopten Endpunkt `GET /v1/workflows/history/<exec>/file` (gated: Lauf hat Pfad berührt; Upload ist in _workflow_run_paths classified=input, also erlaubt). Neuer Helfer `_attachDownloadUrl(a)` → gescopte URL bei `a.wfExecId` (aus chat.workflow_run_id/wfBanner.execId), sonst allgemein; alle 4 Download-Stellen (body-fetch/Bild/PDF/Download-Button) nutzen ihn. Rein Frontend, kein Restart. net-globals-Baseline +1 (neuer Helfer).

Verifikation: End-to-end gegen Live-Server (Seed-Endpoint POST /v1/workflows/history/<exec>/session → 1 input + 7 outputs; content-Endpoint JPG type=image/base64) + Playwright-E2E (JPG rendert als <img data:image/…>, 0 code-Blöcke im Panel). Purge-Guard mit Kontroll-Session verifiziert. Siehe [[project_workflow_run_chat_view.md]], [[project_multimodal_degrade.md]].
