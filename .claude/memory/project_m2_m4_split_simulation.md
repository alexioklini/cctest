---
name: project_m2_m4_split_simulation
description: "Trennungssimulation Win11↔Mac-mini-M4 auf dem realen M2/M4-Setup durchgeführt (2026-07-18): vllm-metal weg, oMLX+gemma-4-12B-oQ4 auf M4, alle Win11-Dependencies (Infinity-Reranker/Whisper-STT) auf M4, M2-Live-config komplett remote — Chat/Embedding/Reranker/OCR laufen über M4, keine Degradation verifiziert"
metadata: 
  node_type: memory
  type: project
  originSessionId: 0708d43c-819d-4d73-a75c-2622be7925dd
---

Am 2026-07-18 auf den PRODUKTIVEN Rechnern (Mac Studio M2 = Brain-Server, Mac mini M4 = Inferenz-Box) die Win11↔Mac-mini-Trennung mit dem bestehenden Setup simuliert. SSH zum M4: `alexander@192.168.1.214` (schlüsselbasiert, ed25519). Host `DGX-Mac`, Mac16,10 (M4), 24 GB RAM, macOS 27.0.

**M4-Zustand jetzt (alle launchd-persistent, KeepAlive):**
- **oMLX** (`com.brain-agent.omlx-serve`, Port 8000): native App `/Applications/oMLX.app` v0.5.1 (per rsync vom M2 kopiert — Signatur ging verloren, egal, `omlx-cli serve` läuft trotzdem). Headless via `omlx-cli serve --host 0.0.0.0 --port 8000 --api-key brain` (NICHT `omlx start` — das will die GUI-App via control.sock steuern → "Failed to control"). Multi-Modell-Server, entdeckt Modelle aus `~/.omlx/models/` (verschachtelte Pfade wie `avneetsb/…` werden als model_id erkannt). Bedient Chat+Embedding+OCR in EINEM Dienst. **Fallstrick behoben**: kopierte `settings.json` hatte `ssd_cache_dir: /Volumes/Scratch/…` (M2-Volume) → PermissionError, auf `~/.omlx/cache/ssd` umgebogen.
- **Modelle auf M4** (nur die 3 gebrauchten, per rsync): `avneetsb/gemma-4-12B-it-qat-oQ4-fp16` (6,7 GB Chat), `mlx-community/embeddinggemma-300m-bf16` (0,6 GB), `GLM-OCR-8bit` (1,5 GB). RAM mit gemma geladen: oMLX ~8,9 GB RSS, ~7 GB inaktiv frei — reichlich.
- **Infinity-Reranker** (`com.brain-agent.infinity-reranker`, Port 8002): `~/.venv_infinity` (Python 3.12 via `/opt/homebrew/bin/python3.12`), `infinity-emb[all]` + **Pflicht-Pins optimum<1.24 + click<8.2** (installiert: 0.0.76/1.23.3/8.1.8). `infinity_emb v2 --model-id BAAI/bge-reranker-v2-m3 --port 8002 --host 0.0.0.0`. Lädt Modell beim Start ~100s. Score-Test korrekt (identisch=0.999, irrelevant~0). Log-Warnung "BetterTransformer not available for MPS" ist HARMLOS (genau dafür der optimum-Pin).
- **Whisper-STT** (`com.brain-agent.whisper-stt`, Port 8001): `~/.venv_stt` (3.12) + `mlx-whisper`, mein Wrapper `packaging/macmini/whisper_stt_server.py` unter `~/.omlx/`, Modell `whisper-large-v3-turbo`. **Fallstrick behoben**: mlx_whisper ruft `ffmpeg` per bloßem Namen → launchd-Env-PATH um `/opt/homebrew/bin` ergänzt (ffmpeg 8.0.1 liegt dort, aber nicht im Default-PATH). Ohne PATH: leere Transkription (Exception abgefangen). Mit: "The Quick Brown Fox" korrekt.
- **sparkdash** (`com.brain-agent.sparkdash`, Port 8013): vllm-Registry `~/sparkdash-instances.json` auf `[]` geleert (Watchdog liest live → kein Neustart). Erweitert (finale Version 3166 Zeilen, auch im Repo `sparkdash/sparkdash.py`): (1) Reranker+STT als **vollwertige Kacheln IDENTISCH zu oMLX** — `class="card vllm omlx"` + `.ihdr` (dot·name·model·state) + `.vgrid`-Stat-Raster, `#depspanel{grid-column:1/-1}` für volle Breite (sonst schmaler als oMLX — User-Reklamation). Backend `_reranker_status()` (Infinity `/models`+`/metrics`: model/backend/capabilities/queue/batch/requests_total via Prometheus `http_requests_total{handler="/rerank"}`) + `_stt_status()` (STT-Wrapper `/status`). (2) **Activity-Log-Integration**: `_dependency_activity_poller`-Thread (3s) erkennt requests_total-Deltas → emittiert Activity-Event `{instance:"Reranker"/"Speech-to-Text", endpoint, detail:"N call(s)"}` in dieselbe `_activity`-deque wie oMLX. **BUG gefixt**: `tickActivity` rief `r.gen_tps.toFixed(1)` — bei Reranker/STT-Events ist gen_tps undefined → JS-Crash → GANZE Tabelle leer ("no LLM calls recorded yet"). Fix: `num()`-Guard + detail-only-Zeilen spannen die 5 Metrik-Spalten via colspan. **STT-Wrapper aufgebohrt** (packaging/macmini/whisper_stt_server.py): `/status`-Endpunkt + Live-Metriken (requests_total/failed, model_loaded, last_duration_s, total_audio_s, uptime). (3) **Eigener Modell-Bereich pro Dienst** (wie oMLXs "Loaded models"): `.omlx-models`/`.omlx-row` mit Modell-ID monospace · loaded-Badge · Größe rechts; Modell-ID aus der Header-Zeile entfernt (wandert in den Bereich, wie oMLX). Größe via `_hf_model_size_bytes()` aus `~/.cache/huggingface/hub/models--<org>--<name>/`. **BUG gefixt**: HF-Cache hält Gewichte in blobs/ + snapshots/-Symlinks → naives Aufsummieren zählt DOPPELT (Reranker 4.6 statt 2.1 GB) → Dedup per realpath-Set. Reranker 2.1G / Whisper-turbo 1.5G (GB=1e9 wie oMLX). Visuell per Safari-Screenshot verifiziert (alle 3 Kacheln gleich inkl. Modell-Bereich+Größe, Activity zeigt Reranker/STT-Zeilen). Finale Version auch im Repo `sparkdash/sparkdash.py`. Backups auf M4: `~/sparkdash.py.bak-*`.

**vllm-metal ENTFERNT**: lief als `.venv-vllm-metal/bin/vllm serve Qwen2.5-7B` (Port 8012), von sparkdash supervidiert (nicht launchd — die Plist war schon `.retired-by-sparkdash`). Registry geleert + SIGTERM an Prozessgruppe (NIE SIGKILL). venv (1,7 GB) + Qwen-7B-Modell (4,0 GB) gelöscht. **Fehlalarm-Lektion**: nach SIGTERM sah ich 8s später noch einen Prozess auf 8012 → das war der STERBENDE vllm (EngineCore braucht Sekunden), NICHT ein Watchdog-Neustart — 15s später stabil tot. Erst prüfen ob's der sterbende Prozess ist, bevor man "Watchdog startet neu" schließt.

**M2-Live-config umgestellt** (Backups: `config.json.bak-m4split-20260718-071523` + Plist-Backup gleicher Stamp):
- config.json: `providers.Lokal.base_url` → `http://192.168.1.214:8000/v1`; `mempalace.reranker` device=remote + url `:8002`; `ocr.mlx_ocr_url` → `:8000` + api_key brain (engine bleibt mlx_ocr — mein v9.377-OCR-Remote-Code postet an oMLX-Vision, GLM-OCR wird direkt bedient, KEIN separater Wrapper nötig!); `mempalace.embedding_*` remote-Keys; `local-mlx-whisper` type in_process→openai base_url `:8001/v1`, fallback_model→whisper-large-v3-turbo.
- **KRITISCH — launchd-Plist MUSS auch geändert werden**: `MEMPALACE_EMBEDDING_DEVICE=mlx→remote` + URL/Model/API-Key ergänzt. Grund: server.py seedet die MEMPALACE_*-Env per `os.environ.setdefault` → die Plist-Env GEWINNT über config.json. Nur config.json ändern hätte Embedding still in-process gelassen.
- Server GRACEFUL neu (bootout/bootstrap, SIGTERM — nie kickstart -k, das SIGKILLt + korrumpiert HNSW; ein Safety-Hook blockte meinen -k-Versuch korrekt).

**Verifiziert (keine Degradation)**: Chat-Turn über Brain-API → "Paris"/"Hallo" von gemma auf M4 (M4:8000-Verbindung live beim Turn beobachtet, queue_wait→queue_acquired); Embedding `embed_query()` → 384-dim (MRL-Truncation 768→384 normal) gegen M4, kein LATCH; Reranker device=remote; OCR `mlx_ocr.extract()` → "BELEG 2026-42" über M4. Server-Log KEINE Fehler/LATCH/Fallback. Server-Prozess-Env bestätigt EMBEDDING_DEVICE=remote.

**Doctor + Externe-Bibliotheken remote-bewusst gemacht** (2026-07-18): (1) `engine/doctor.py` — NEUE Live-Checks in `run_live_checks`: `live_rerank` (bei reranker.device=remote → POST url/rerank, Ausfall=FAIL), `live_ocr` (bei ocr.engine=mlx_ocr+mlx_ocr_url → GET url/v1/models, Ausfall=FAIL), und die bestehende Embedding-Probe zeigt bei remote-Ausfall jetzt FAIL statt WARN + "remote (Mac mini)" im OK-Text. **ROOT-BUG gefixt**: `doctor._cfg()` gab nur `brain._server_config()` zurück, das mempalace/ocr/searxng/crawl4ai NICHT enthält (server.py Boot kopiert nur einzelne scalar-Keys, Zeile ~3750ff) → alle meine Checks sahen None → wurden übersprungen. `_cfg()` liest die 4 Sektionen jetzt zusätzlich direkt aus config.json (json-Import ergänzt!). Fehler-Test verifiziert: Reranker auf M4 gestoppt → Doctor overall=fail, live_rerank=❌ "Connection refused". (2) `engine/lib_versions.py` + `web/js/settings_general_tabs.js` — die "Bibliotheken"-Ansicht (Endpoint /v1/lib-versions, Tab settings_general.js) zeigt bei remote-Betrieb die MLX-Gruppe (mlx/mlx-metal/mlx-lm/mlx-vlm) als status="remote" statt grün-"ok": neue `_remote_inference()` liest config.json → wenn embedding/reranker/OCR remote, bekommt JEDE MLX-Zeile status=remote + Notiz "installiert, aber ungenutzt — läuft remote auf <IP>" (User-Frage: lokales mlx wird NICHT mehr genutzt, nur noch installiert), Version durchgestrichen; Gruppen-Notiz "Inferenz ausgelagert (...) auf <IP>". Frontend rendert status=remote als amber var(--warning), nicht rot var(--error). Verhindert das False-Rot auf Win11 (keine lokale MLX) UND das irreführende False-Grün auf dem M2 (installiert aber ungenutzt).

**Rollback** (M2 zurück auf lokal): `cp config.json.bak-m4split-20260718-071523 config.json`, Plist-Backup zurück, Server bootout/bootstrap. Dann läuft M2 wieder komplett in-process.

**Offen/Beobachten**: STT-Fallback über M4 noch nicht mit echtem Audio über die Brain-API getriggert (nur Wrapper-Ebene getestet — Primär ist Cloud-Voxtral, Fallback selten). Qdrant/SearXNG/crawl4ai bewusst NICHT auf M4 (laufen im Voll-Profil lokal auf dem "Client" M2 — korrekt). Bezug: [[project_windows_deployment_package]] (der Win11-Installer, den diese Simulation nachstellt), [[feedback_never_sigkill_brain]], [[reference_brain_admin_login]].
