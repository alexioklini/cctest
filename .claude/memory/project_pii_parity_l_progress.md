---
name: project_pii_parity_l_progress
description: "PII-Analyse-Parität (L1-L7 aus PII_ANALYSIS_PARITY_HANDOVER.md) — KOMPLETT geliefert inkl. Live-E2Es; nur Nebenbefund-Katalog offen"
metadata: 
  node_type: memory
  type: project
  originSessionId: 2d8ec3f6-686b-4e71-b8e3-f3ee8284d164
---

Stand 2026-07-14: PII_ANALYSIS_PARITY_HANDOVER.md (im Repo eingecheckt, 2f1fa8ce) wird umgesetzt. Reihenfolge laut Handover §3: L1 → L3 → L2 → L4 → L5 → L6 → L7, plus Sofortmaßnahme §5 zuerst.

**Geliefert:**
- **v9.334.0 (2f1fa8ce)** — Sofortmaßnahme = L4 Phase 1: `brain._gdpr_guard_web_args` am Dispatch-Choke-Point (`llm_loop.dispatch_tool`), Config `gdpr_scanner.web_egress` (refuse default | ask=refuse bis Phase 2 | block_group | allow), Clamp-Satz („nicht prüfbar (Datenschutz)"), block_group via exclude_tools im Worker, Audit pii_web_blocked/pii_web_egress, tests/test_web_egress_gate.py (17).
- **v9.335.0 (e2395b97)** — L1: Tool-Gruppe `doc_checks` (mrz_verify/doc_dates_check/identity_consistency, engine/tools/doc_checks.py) + `engine/identity.py` (Namens-Normalisierung, **L2 muss dieses Modul wiederverwenden**). Am echten 10-JPG-Satz validiert. tests/test_doc_checks.py (37).
- **v9.336.0** — L3 Dispatch-Symmetrie komplett: L3a `brain._gdpr_deanon_tool_args` + `GDPR_ARGS_DEANON_TOOLS` (24 Tools) in `dispatch_tool` NACH dem Web-Gate; **Härtung über den Handover hinaus:** execute_command/python_exec-Strings mit Netzwerk-Markern (curl/https:///urllib/…) behalten Fakes (Seitentür-Egress). L3b Seams: mempalace_query + kg_query/search/**neighbors** (v9.96.0-„stay raw" revidiert), web_fetch (Wrapper `_web_result_anon`, nur content-Feld), _searxng_query (beide Returns), exa_search; Klassifikations-Gate deckt damit auch mempalace/Web (Daemons safe — ohne Session-Modell no-op); _build_web_sources fängt GDPRBlockedError pro Quelle + erbt den Seam (F5 teilgeschlossen). L3c Notice-Split im Ledger-Rewrite. tests/test_dispatch_symmetry.py (16, inkl. Whitelist∩Web=∅-Invariante).

- **v9.338.0** — L4 Phase 2 (`ask`-Modus) komplett: `_gdpr_guard_web_args` gibt jetzt `(refusal, args)` zurück (Callsite dispatch_tool). Consent per WERT via `brain._web_consent_ask` (EIN AskUserQuestion-Batch über die bestehende ask_user-Mechanik + Frage-Karte — null neue UI; Slot VOR Emit registriert; non-interactive → refuse `no_consent`; EIN unbeantworteter Dialog pro Turn via `_web_consent_asked` auf dem RequestContext). Ledger: `turn_action=release_web/deny_web` mit **value-only namespaced Hash** `_web_release_hash`=sha256('web|'+norm) — NIE sha256(rule_id|value), sonst shadowt die Consent-Zeile die anonymise-Zeile (Wire-Rewrite verlöre fake_value = Leak); Reader `ChatDB.get_session_web_releases`. Hin-Übersetzung `_web_release_translate_args` (Fake→Original NUR dispatch-seitig, Slug-aligned); Rück-Anon = L3b-Seam. Variant-Intersection beim Released-Matching (Freigabe deckt L2-Varianten; Deny gewinnt). GDPR-Modal: Status web_released/web_denied + per-Zeile-Toggle (`_status_of` in pii-decisions-view mappt die neuen Actions — neue turn_actions IMMER dort mitziehen; history_edit-Save ist call-weit → Web-Toggles schreiben eigene Calls). tests/test_web_egress_gate.py 17→28. **Live-E2E (Browser-Rundlauf ask-Modus) steht aus — beim L5/L7-Test mit erledigen.**
- **v9.337.0** — L2 Entitäts-konsistente Pseudonymisierung komplett: `Mapping.entities` (Buchführung; forward/reverse bleiben Arbeitsspeicher — Varianten als ECHTE Einträge registriert, §7.9 ✓ → L3a + Web-Gate automatisch entitäts-fähig), `engine/identity.py` erweitert (entity_attach 3-stufig, GARBLE_FLOOR 0.60/ANCHOR 0.72; render_variant token-weise formtreu; standard_variant_pairs inkl. Glued-Givens), Seeding in TEXT-Reihenfolge vor dem end-absteigenden Splice (sonst seedet Garble die Entität), Garbage-Guards (Nicht-Namens-Tokens/>3 Vornamen → keine Entität; learn skippt >4-Token-Spans), passport/dob/mrz in SHAPE_PRESERVING, neue Scanner-Regel `mrz` (nur ^-Anker, Struktur-Validator), `_fake_mrz` mit ALLEN ICAO-Prüfziffern neu gerechnet (Expiry unverändert, DOB per konstantem Session-Offset ±5..25d statt Tag-Jitter — Deltas exakt), textuelle Monate+EXIF in Patterns+Scanner, `apply_known_values`-Sweep im `_gdpr_anon_tool_text`-Seam. LIVE verifiziert (ko-kunden, glm-5.2): Fake-Query im Wire, Drawer gefunden, Klarnamen-Pfad lesbar, Familie distinkt — damit auch die offene L3-Live-Verifikation erledigt. tests/test_pseudonymizer_entities.py (15).

- **v9.339.0** — L5 komplett: **L5a** OCR-Block (`_OCR_BLOCK_MARKER`) wird VOR der Pfad-Notice gebaut → scannbare typed-Hälfte; `_split_attachment_notice` zieht bei Legacy-History den Block aus der Notice (wire-only Reorder) → Scan+Ledger-Rewrite decken auch Altbestand; Pfade bleiben exempt. **L5b** `brain._gdpr_seed_entities_from_attachments` (Worker, vor dem Text-Scan): `_ocr_mrz_strip`+`parse_mrz` je Bild/PDF → `pseudonymizer.seed_identity_from_mrz` (Name braucht ≥2 verifizierende Prüfziffern; Passnummer bare+10er; DOB-Formvarianten). **Beste-Lesung-zuerst** (Checksummen-sortiert — Dateinamen-Sortierung ließ das 1-Prüfziffern-Foto `BONNTIMARTI` die Entität vergiften); Zweitlesung derselben Nummer legt NIE eine neue Entität an. **L5-Sweep:** NEU `apply_entity_variants` (Fuzzy-Fenster 2-5 Cap-Tokens, Unterstrich/Slash NIE Separator → Pfade heil; lernt nie; registriert Ersetzungen als forward/reverse → L3a-Roundtrip; Stufe 3 = ALLCAPS-Substring nur in `<<`-Zeilen) VOR `apply_known_values` an BEIDEN Stellen — auch NEU im Worker-typed-Pfad (damit NER-Wortstellungs-Lücke `Stark Bonnie` GESCHLOSSEN); `apply_known_values` sweept jetzt auch passport/dob; ALLCAPS-Nachname-allein als Variante. Gemessen am 10-JPG-Satz: NULL Klarwerte in der typed-Hälfte (nur der Dateiname im exempten Pfad bleibt — per Design). tests/test_mrz_entity_seed.py (17).

**Wichtige Implementierungs-Erkenntnisse:**
- NER-Wortstellungs-Lücke: `"Stark Bonnie …"` (Nachname zuerst) taggt die deutsche NER nur als `Stark` → Vorname bleibt roh. **Seit v9.339.0 durch den Entity-Sweep im Worker-typed-Pfad geschlossen.**
- cz_rc (Checksummen-Regel) matcht bare 9-stellige US-Passnummern → `_registered_id_fake` spleißt opake Fremd-Tokens NIE in MRZ.
- Live-Tests mit Scanner brauchen TEMPORÄR `enabled:true`+`name:warn` in config.json — exakt revertieren; test_pii_ner liest die Live-Config und failt sonst.
- `_check_tool_dedup` wird im Live-Dispatch-Pfad seit 9.247.0 NICHT aufgerufen (nur totes `_execute_tool_inner`) — engine/CLAUDE.md-Doku-Drift, unangetastet.
- MRZ auf Handyfotos: generischer OCR-Pass liefert NULL parsebare Zeilen; erst `_ocr_mrz_strip` (tesseract-Whitelist A-Z0-9<) funktioniert. Checksummen selbst-validieren die beste Lesung. WebID-Screenshots im Referenzsatz haben die MRZ ABGESCHNITTEN → mrz_found=false ist dort korrekt.
- Ehrlichkeits-Invariante: unlesbares MRZ-Feld ⇒ null, nie false (sonst F2 in Gegenrichtung).

- **v9.340.0** — L6 komplett: **L6b** NEU `pseudonymizer.lint_residual_fakes` (4 Klassen: token_remnant salt-matched+saltlos-für-geminte-KINDs, exact_fake wortgegrenzt — fängt auf Dateien docx-Run-Splits/xlsx-Formeln; token-förmige Keys kein Doppel-Report, reformatted_date via `_parse_date_surface`+Alternativ-Formen DE/EN, name_genitive/name_initials aus entities — Genitiv nur wenn Nachname+s kein reverse-Key). Verdrahtet: Reply-Seam (unrestored im Synthetic-Result + metadata.gdpr_unrestored + „⚠️ Datenschutz-Hinweis"-Block ans Reply, Nudge-Muster) + File-Seam (`_lint_written_file` nach deanonymize_file). **L6a ENTSCHIEDEN: Steuern+Fail-loud statt HTML→PDF-Render** (kein HTML→PDF-Renderer existiert; report_html.py ist nur MD→HTML; crawl4ai optional/per-machine): `.pdf` LINT-ONLY im After-Write-Callback → Row status=error + Audit `pii_report_fidelity` + Modell-Warnung via NEUES RequestContext-Feld `_gdpr_file_warnings`, gedraint in `llm_loop.dispatch_tool` (der EINE Choke-Point aller 3 PDF-Quellen: write_document schreibt direkt .pdf!, python_exec, execute_command); sauberes PDF still. **L6c** Clamp: „Report fidelity" (rechnen ja — reformatieren nein) + PDF-Verbot (.html/.md stattdessen). Golden am echten 67KB-Report: forward→reverse→Linter 0; anonymisierte Form (PDF-Simulation) 12 Findings. tests/test_report_fidelity.py (20).
- **Nebenbefund (L2-Edge, offen):** `5 FEB 1947` vs `05 FEB 1947` kollidieren auf denselben Fake wenn der Offset den Tag zweistellig macht → Roundtrip vereinheitlicht das Padding (Wert korrekt). Bei L7-Aufräumen mitnehmen.

- **v9.341.0 + v9.341.1 — L7 komplett, SERIE ABGESCHLOSSEN:** **L7a** `project.json → gdpr_preset` (''|'kyc'|'kyc_local'; Editor in Projekt-Einstellungen) — Overlay per KOPIE auf der gecachten Global-Config (`_gdpr_apply_project_preset`; unkeyed 30s-Cache NIE mutieren!), Auflösung `preset=`-Param (HTTP-Threads; **`project_name or session.project` — beim ERSTEN Send ist session.project leer, der 9.341.1-Fix**) > RequestContext `gdpr_project_preset` (apply_domain_context → build_tool_context → `_apply_bg_context`) > global. kyc = enabled + web_egress ask + `rule_overrides.name→warn` only-strengthen + doc_checks undeferred + **Auto-Anonymise ab Turn 1 ohne Modal** + research_mode-Kopplung; kyc_local = Force-Swap aufs Fallback-Modell (fail-loud 400 ohne) + bg swap_to_local. Client: GET /messages liefert `gdpr_project_preset`, sendMessage skippt das Modal. **L7b** RequestContext-Feld `_gdpr_degradation` (Counts, nie Werte; getallied in `_web_gate_audit`/dispatch_tool(doc_checks)/PDF-Callback) → `metadata.gdpr_degradation` → Schild-Streifen „Datenschutz dieser Antwort" in chat_render.js. **L7c** web_egress-Select in Settings→GDPR (Save-Validierung `_WEB_EGRESS_MODES`; Services-GET liefert den Key jetzt). js_gate-Baseline 1995→1996 (setProjectGdprPreset). tests/test_gdpr_project_preset.py (22).
- **Live-E2Es GEFAHREN (Session 7, Projekt ko-kunden, globale Config blieb unangetastet — das Preset war der Test):** L7 ✓ (kein Modal, Mapping ab Turn 1), L4-P2 ✓ (Consent-Karte pro Wert, 5 release_web, Suche lief rückübersetzt, Fake-Query-Hin-Übersetzung „Cameron Taylor", nicht freigegebene Werte refused, „nicht prüfbar (Datenschutz)"-Wording, **Widerruf im Modal → deny_web**), L7b ✓ (Streifen rendert), L6 ✓ (Modell verweigerte PDF explizit + schrieb bericht.html, File-Seam restaurierte alles), L5 ✓ (JPG-Seed: `Bonnie Marie Stark→John Quinn Turner` inkl. MRZ-Zeile + Middle-Initial-Formen ab Turn 1). Aufgeräumt: Sessions gelöscht, Preset+research_mode exakt revertiert, verwaiste pii_decisions gepurged.

- **v9.342.0 — Nebenbefund-Katalog abgearbeitet** (auf User-Wunsch alles außer Kilo-400 + Stream-Stall): (1) NER-RECALL-NETZ: `de_core_news_sm` als enge PERSON-Union neben md (`recall_model` in KNOWN_LANGUAGES, `<lang>#recall`-Cache; ≥2 kapitalisierte Tokens, kein Overlap, `_RECALL_STOP_TOKENS` gegen Title-Case-Prosa) — Root-Cause reproduziert: **md verpasst 'Bonnie M Stark' im getippten Satz, sm fängt es**. (2) `ChatDB.delete_session` purgt jetzt pii_decisions mit. (3) `render_variant`: Einzelbuchstabe nach Apostroph = Klitik-Suffix, verbatim (`Taylor's` statt `Taylor'm`). (4) `Mapping.record`: Padding-Kollision desselben Datums → reverse behält deterministisch die gepaddete Form (beide Reihenfolgen; Nicht-Datum bleibt last-wins). BEIFANG: test_web_egress_gate hatte einen VORBESTEHENDEN Ordnungs-Flake (spaCy-Modelle im Prozess → Gate-Zusatz-Scan sieht mehr → Ask-Tests kippen; auf HEAD verifiziert) — `_GateTestBase` isoliert jetzt `_NLP_CACHE`. tests/test_l7_cleanup_fixes.py (14).

**Bewusst offen gelassen (User-Call):** Kilo-Multimodal-400 bei 4MB-JPG (gehört zu _sanitize_multimodal_for_model) · Upstream-Stream-Stall ~10min ohne Timeout (httpx-Read-Timeout prüfen).
