---
name: project_windows_deployment_package
description: "Windows-11-Deployment-Package (Bank-Testausrollung) — v9.366 Architektur/Sizing 10/70 User + Setup v2 (84-KB-Bootstrapper, Komponenten-Delta-Updates, Minimal-Profil, Offload-Analyse); offene Win-Verifikation"
metadata: 
  node_type: memory
  type: project
  originSessionId: 07baef98-fc9f-417f-8f4f-cc3219baa2d5
---

v9.366.0 (2026-07-17): Windows-11-x64-Package für die Bank-Testausrollung gebaut (`packaging/win/` komplett modernisiert — build_win.sh, install.ps1, BrainAgent.bat.tmpl, stop.bat.tmpl, installer.nsi/NSIS-setup.exe via `brew install makensis`). Windows-Client = MEHRBENUTZER-Server (0.0.0.0:8420); alle LLMs auf dem Mac mini M4 via oMLX `http://<ip>:8000/v1` (Provider „Lokal", `is_local:true` — is_model_local ist FLAG-basiert, LAN-Umzug bricht nichts).

User-Entscheide (2026-07-17):
- **Offline-first trotz Internet**: Bank-Firewall kann PyPI/Playwright-CDN blocken → ALLES beim Build vorpacken (Wheels, venv-site für SearXNG/crawl4ai, Chromium-Zips, Qdrant-Binary, ONNX-Embedding-Modell); install.ps1 geht nur bei fehlendem Asset online.
- **mempalace: lokale Venv-Änderungen reisen IMMER mit** — nicht nur der int8-Patch: das komplette `~/.mempalace/venv/.../mempalace/` wird beim Build über die pip-Kopie gersynct (Anker-Pin 3.4.0 zieht nur die Deps). Gilt für jedes künftige Deployment.
- **Sizing**: Testausrollung 10 gleichzeitige User, Endausbau 70. Qdrant lokal auf dem Client, int8-Quantisierung PFLICHT (`MEMPALACE_QDRANT_QUANTIZATION=int8`; 70×10k Drawer ≈ 0,6 GB statt 2,5 GB auf 16-GB-Box). Für 70 User Hardware-Review nötig (Mac-mini-Inferenz + CPU-Embedding sind die Engpässe).
- Endprodukt: setup.exe (NSIS, RequestExecutionLevel user, kein Admin) + GitHub-Release (Repo alexioklini/cctest).

Nicht-offensichtliche Technik:
- Windows-Bundle = **Python 3.13** (spaCy hat NIRGENDS cp314-Wheels; cp313 hält den GDPR-NER am Leben). Mac-.app bleibt 3.14 → spacy-Einträge in packaging/common/requirements.txt sind win32-gegated.
- Playwright 1.60 chromium/win64 = **Chrome-for-Testing**: `cdn.playwright.dev/builds/cft/<browserVersion>/win64/chrome-win64.zip` (+ chrome-headless-shell) — NICHT `builds/chromium/<rev>/…` (400er). Install-Ziel `%LOCALAPPDATA%\ms-playwright\chromium-<rev>\` + INSTALLATION_COMPLETE-Marker.
- Embedding (v9.367.0): **remote auf dem Mac mini** — `mempalace.embedding_device: "remote"` + `embedding_url` in config.json (server.py seedet MEMPALACE_*-Env per setdefault), oMLX `/v1/embeddings` mit `embeddinggemma-300m-bf16` (Parität cos=1.0 zu MLX; Patch 2d in [[project_mempalace_venv_patches]]). Fallback: latcht prozessweit auf CPU-ONNX (`onnx-community/embeddinggemma-300m-ONNX` bleibt im Bundle-hf-cache, `HF_HUB_OFFLINE=1`). Reranker (torch) + KG im Windows-Seed AUS.
- SearXNG läuft aus dem vendorten Checkout via `python -m searx.webapp` mit cwd im Checkout — kein editable-Install nötig (nur Deps im Venv).
- Windows-Codefixes (Phase A): kill_process_tree (taskkill /T /F — os.killpg/SIGKILL fehlen auf Win, AttributeError den except-OSError NIE fing), MCP-stdio Reader-Thread statt select-auf-Pipe, brain_tmp_root()/brain_attachments_dir() (POSIX bleibt WÖRTLICH /tmp — gettempdir() wäre auf macOS /var/folders = stiller Prod-Pfadwechsel), terminal.py PTY_SUPPORTED-Guard.

OFFEN: echter Test auf Win11-Hardware/VM (Boot, Chat gegen Mac mini, mempalace-Offline-Embedding/Palace-Bootstrap, SearXNG/crawl4ai-Spawn, Kill-Pfad, stdio-MCP, GDPR-NER, 10-User-Smoke) — Testplan in brain.py-CHANGELOG 9.366.0 + Plan-File. Prüfpunkt: `-m searx.webapp` ohne pip-Metadaten (Versions-Lookup) unter Windows.

**Setup v2 (2026-07-17, packaging-only-Commit, kein VERSION-Bump):**
- **setup.exe = 84-KB-NSIS-Bootstrapper** (installer.nsi NEU, MUI2/nsDialogs): Modi Neuinstallation/Update AUTODETEKTIERT (`app\.version` im InstallDir), Quelle Offline-Payload (Zip ODER Ordner, autogefunden neben der exe) oder Online-URL (editierbar, Default GitHub latest); Silent `/S /MODE= /SOURCE= /PAYLOAD= /URL= /NODEPS /MINIMAL`. Gesamte Logik in `setup_stage1.ps1` (PS-5.1-kompatibel, im exe embedded — exe muss nur bei Installer-LOGIK-Änderung neu verteilt werden, SETUP_VERSION).
- **Komponenten-Architektur**: build_win.sh zerlegt das Bundle in 5 content-addressed Zips `<name>-<sha12>.zip` (app 10M / python 321M / websearch=venv-site+browsers 503M / qdrant 29M / hfcache 432M) + `BrainAgent-win-manifest.json` (Vertrag mit stage1: name/file/sha256/size/required/dirs). Payloads: `-payload.zip` (1,2G airgap voll, zip -0 aus Hardlinks) + `-payload-app-only.zip` (10M = leichtes Versions-Update). **mtime-Determinismus ist Pflicht** (rsync -a statt cp -R, cp -p für Browser-Zips, `touch -t` auf generierte revisions.txt) — sonst kippen die shas pro Build und Delta-Updates laufen leer.
- **Update-Mechanik** (stage1): Receipt `components.json` (installed-shas + skipped) → sha-Vergleich VOR Download/Kopie, nur Geändertes anwenden; `.prev-Swap` + Rollback; stop.bat vor Apply; python- oder websearch-Wechsel ⇒ `install.ps1 -RecreateVenvs searxng,crawl4ai` (Venvs zeigen auf bundle\python). Migration von 9.366/67-Monolith-Installs: erstes Update lädt mangels Receipt einmal alles, danach Delta. `-UpdateDeps 0` hält geänderte Deps bewusst zurück (App-only).
- **Online-Quelle**: github.com/alexioklini/cctest ist PUBLIC → `releases/latest/download/<asset>` ohne Auth; `publish_release.sh` (NUR manuell) lädt setup.exe+Manifest+Komponenten+Payloads als Release `win-v<ver>`. Fallstrick: latest = neuestes Release des GANZEN Repos — sonst explizite URL `releases/download/win-v<ver>` im Assistenten. Download via curl.exe (Win11-Bordmittel, HTTPS_PROXY respektiert), Fallback IWR+Tls12.
- **Minimal-Profil** (Checkbox Neuinstallation): websearch/qdrant/hfcache weg → ~0,9 statt 2,1 GB. install.ps1 patcht (NUR freshConfig) searxng/crawl4ai auf `http://<mini>:8088/8422` + auto_start:false und schreibt `brain-env.bat` ins DATA-Verzeichnis (MEMPALACE_QDRANT_URL remote + BRAIN_QDRANT_LOCAL=0; überlebt Updates, BrainAgent.bat call't sie VOR den Defaults und skippt den Qdrant-Start). Mac-mini-Dienste dafür: MACMINI_SETUP.md §6 (SearXNG 8088, crawl4ai 8422, Qdrant 6333, alle 0.0.0.0). Profilwechsel nachträglich = Neuinstallation oder config-Hand.
- **Offload-Analyse** `packaging/win/WIN_FOOTPRINT_ANALYSIS.md` (reist im Bundle mit): 60 %/1,3 GB heute per Config auslagerbar (searxng.url/crawl4ai.url/MEMPALACE_QDRANT_URL sind reine HTTP-Seams, Supervisoren auto_start-gegated); GDPR-NER bleibt LOKAL (fail-closed-Gift bei Mini-Ausfall); Follow-up offen: site-packages-Trim ~150M (chromadb/kubernetes via mempalace-Anker `--no-deps` ersetzen).
- **Reranker remote (v9.376.0)**: `reranker.device:"remote"` + `url` in engine/mempalace_glue.py → `_RemoteReranker` (Infinity `POST /rerank`, CrossEncoder-predict-API; Latch nach 2 Fehlversuchen wie beim Embedding-Fallback — toter Endpoint kostet nicht jede Suche einen Timeout). Score-Skala IDENTISCH zum lokalen Pfad (sentence_transformers gibt für bge-reranker ebenfalls sigmoid-normierte Werte zurück — NICHT rohe Logits; live verifiziert max |diff| = 0.000000). KEIN mempalace-venv-Patch nötig (der Cross-Encoder-Reranker ist komplett brain-seitig). Win-Seed: AN/remote `MACMINI_IP:8002` (install.ps1 ersetzt IP). Mac-Produktionspfad (device auto) unverändert. **Infinity-Pins PFLICHT** (Stand 07/2026): `optimum<1.24` (bettertransformer entfernt → Import-Crash) + `click<8.2` (typer-CLI-Bruch „Secondary flag…") — Rezept in MACMINI_SETUP.md §4. 5 Tests in tests/test_reranker_remote.py.
- **Verifiziert auf macOS**: 33 Artefakt-Checks (Manifest/sha/Payload-Entries) + 5 E2E-Läufe der ECHTEN setup_stage1.ps1 unter portablem pwsh 7.6 mit powershell.exe/cmd.exe-Shims (Voll-Install, No-op-Update via app-only, Minimal, Skip-Persistenz beim Update, Delta-Update mit Swap+Receipt). Dabei realen Bug gefixt: `\\?\`-Langpfad-Präfix jetzt isWin-gegated (feuerte hostunabhängig ab 248 Zeichen). OFFEN zusätzlich zum alten Testplan: setup.exe-GUI/Silent + Online-Modus + Update-Pfad auf echter Win11-Maschine; noch kein GitHub-Release publiziert.

**Parität-Audit + Fixes (2026-07-18) — „läuft ohne Abstriche wie auf M2":** Zwei Explore-Sweeps (Tool-Pfad + Server/Daemons) + zwei Paritäts-Sweeps (was der Server braucht ↔ was Installer/MACMINI_SETUP.md einrichten). Ergebnis: Codebase schon gut Win-gehärtet, ABER execute_command war KOMPLETT TOT (`_build_shell_command` file_tools.py:3770 baute `[$SHELL or /bin/zsh, -l, -c]` ohne os.name-Zweig → FileNotFoundError JEDER Aufruf). User-Entscheid: **Installer bringt Git-Bash SELBST mit** (nicht voraussetzen).
- **Shell-Fix**: MinGit (`v2.51.0`, Git-for-Windows MinGit-Zip, bash+coreutils+git.exe, ~45M) als NEUE **required** Bundle-Komponente (build_win.sh: Download+Extraktion nach `mingit/`, comp_zip+META required:true — MUSS in beiden Profilen, auch Minimal). BrainAgent.bat.tmpl setzt `BRAIN_SHELL_PATH=%SCRIPT_DIR%mingit\usr\bin\bash.exe` + prependet mingit auf PATH (überlebt Data-Dir-Updates, KEIN tools_config-Patch nötig — tools_config.json wird bei Updates via robocopy überschrieben, daher Env-Weg statt Config-Patch). `_build_shell_command` + brain.py MCP-stdio (:4986) lesen jetzt: config.shell_path → BRAIN_SHELL_PATH → cmd.exe-Fallback (NIE /bin/zsh auf Win). Mac-Pfad byte-identisch. Unix-Tool-Prosa (ps aux/cat/2>/dev/null) bleibt damit gültig → kein Kalibrierungsrisiko.
- **2 os.pathsep-Bugs** gefixt (`":"`→`os.pathsep` in PYTHONPATH-Fuge): file_tools.py:4554 (python_exec), kernels.py:207 (kernel_exec) — venv-Pakete importierten sonst nicht auf Win. **signal.SIGKILL** 4× geguarded (`getattr(sig,"SIGKILL",9)`, file_tools.py — latenter AttributeError auf Win bei negativem returncode).
- **2 echte Paritätslücken OHNE Remote-Code-Weg** (Inventar-Sweep: von allen MLX-Features haben NUR OCR+STT keinen HTTP-Weg): User-Entscheid für beide = **echte 1:1-Parität, kein Cloud**. (1) **OCR**: NEUE `device=remote`-Branch in engine/mlx_ocr.py (`_remote_endpoint`/`_extract_remote`, liest ocr.mlx_ocr_url via doc_convert._ocr_config; POST an OpenAI-Vision `/v1/chat/completions`, Bild base64-inline) + NEUER Wrapper-Dienst `packaging/macmini/glm_ocr_server.py` (stdlib http.server + mlx_vlm, EIN Thread=Metal, serviert GLM-OCR-8bit, Port 8003). Win-Seed: `ocr.engine:"mlx_ocr"` + `mlx_ocr_url:http://MACMINI_IP:8003` (war „none" = OCR still AUS). install.ps1 ersetzt die IP auch für OCR. (2) **STT**: NEUER Wrapper `packaging/macmini/whisper_stt_server.py` (multipart `/v1/audio/transcriptions` → mlx_whisper, Port 8001) — Brain braucht KEINEN neuen Code (HTTP-Whisper via normalen Provider existiert), nur Config+Dienst. Beide Wrapper reisen im app-Bundle mit (build_win.sh comp_zip app + macmini/-Ordner). Schnittstelle Brain↔Wrapper end-to-end getestet (Payload-Format passt exakt).
- **Tote Seed-Referenzen gefixt** in tools_config.sample.json (NUR Windows-Bundle, keine Mac-Vorlage): `CLIProxyAPI/*` (Provider seit v9.278.0 GELÖSCHT!) in refinement.model+translation → leer; `Mistral/`-Provider-Name (heißt mistral-direct) in read_document.vision → leer; whisper-base/voxtral-Audio-Slots dokumentiert (brauchen Mac-mini-Endpoint). **`Lokal-M4`/8012** (vllm-metal bg-Inferencer) ist im M2-config real genutzt (tools_config.json:43 refinement→Qwen2.5-7B), aber im Win-Seed NICHT reproduziert — bewusst leer gelassen (bg-Default greift); falls Win den bg-Inferencer braucht, Provider Lokal-M4 + Modell nachtragen.
- Alles verifiziert: py_compile aller Module, laufender M2-Server (9.376.0) antwortet weiter, `_remote_endpoint()` auf Mac korrekt leer (MLX-Pfad unberührt), config-Heredoc+install.ps1-Python-Patch valide+simuliert (alle 4 MACMINI_IP-URLs ersetzt), Wrapper-Schnittstellen-Parse getestet. OFFEN: Win11-Live + Wrapper-Dienste auf echtem Mac mini starten. NICHT committet (User committet/testet selbst nach [[feedback_defer_to_users_migration_calls]]).
