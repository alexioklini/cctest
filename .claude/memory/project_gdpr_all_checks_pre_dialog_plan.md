---
name: project_gdpr_all_checks_pre_dialog_plan
description: UMGESETZT v9.383.0 — ALLE PII-Checks vor dem Dialog, Worker entscheidungsgetrieben, MRZ = markierbare Findings; Kern-Lektionen: Ledger ist hash-gekeyt → pro-WERT-Recency nötig; Varianten-Zeilen re-minten Entitäten → values_same_subject-Filter
metadata:
  node_type: memory
  type: project
  originSessionId: d895e194-3c1c-4dd3-8670-7c2235d41ca0
  modified: 2026-07-20T15:52:20.883Z
---

**Status: UMGESETZT + LIVE VERIFIZIERT (v9.383.0, 2026-07-20).** Plan-Datei `GDPR_ALL_CHECKS_PRE_DIALOG_PLAN.md` im Repo-Root; Anlass Chat 912d919973fd (4 FP markiert, 3 trotzdem anonymisiert).

**Architektur seit 9.383.0:** Erkennung läuft EINMAL vor dem Dialog (`/v1/attachments/scan` inkl. MRZ-Pass → `mrz_name`/`mrz_passport`/`mrz_dob` als markierbare Findings via `brain._mrz_findings_from_parse`; Kategorie personal, NICHT contact — contact/ignore hätte sie versteckt). Dialog-Entscheidung reist synchron: Client awaitet `recordPiiDecisions` UND schickt `pii_decisions` im `POST /v1/chat`-Body (auf ERSTEM Send einziger Kanal — client hat noch keine session_id). Worker: Ledger∪Body → `purge_value` je FP → `seed_from_decision` je bestätigtem Wert → Sweeps (`apply_known_values` mit `categories=None` = alle; ws-tolerant für Multi-Wort-Keys). `_gdpr_anon_tool_text` ist APPLY-ONLY (kein `_pii_scan_text` mehr). `_filter_pii_false_positives` GELÖSCHT. Fail-loud-Guard: anonymise ohne Decisions + leeres Mapping → Fallback-Vollscan (`guard_scan` im Event).

**Zwei Bugs erst im LIVE-Test gefunden (Unit-Tests grün, 3-Turn-Matrix rot — Lektion: [[project_kg_anonymise_incident]] reproduce before assert gilt auch andersrum):**
1. **Ledger ist (rule_id|value)-HASH-gekeyt** — derselbe WERT trägt widersprüchliche Zeilen (Dialog: FP unter `mrz_name`; Turn-End-Bulk-Recorder: anonymise unter Mapping-Regel `name`). Hash-latest-wins ließ die abgeleitete Zeile das FP-Mark überstimmen. Fix: `_latest_decisions_by_value` (pro-Wert-Recency, `get_session_pii_decisions` liefert jetzt `created_at`; Tie → non-FP gewinnt). Gleicher Fix im Wire-Pass `_apply_pii_decisions_to_wire` (Loch VORBESTAND dort).
2. **Varianten-Zeilen re-minten die Entität**: Turn-End schreibt jede registrierte Variante ('Bonnie Stark', 'STARK', DOB-Formen, Prüfziffern-Zwilling) als EIGENEN Ledger-Wert mit anonymise-Status — nach dem Purge minteten die eine frische Entität, deren Fuzzy-Sweep den FP-Wert wieder fakte. Fix: `pseudonymizer.values_same_subject` (entity_attach/Einzeltoken/Org-Stamm/Kalenderdatum/ID-Zwilling) filtert den Mint-Satz gegen die FP-Werte.

**Wichtige Semantik-Fakten:** DB-User-Zeile hält das ORIGINAL (UI zeigt Echtwerte), Wire-Wahrheit in `metadata.wire_content` — E2E-Checks müssen wire_content prüfen, nicht content. Turn-End-Recorder + Cleartext-Accept-Rescan überspringen FP-Werte (wert-normiert). Token-Stabilität gepinnt (Test gegen `seed_identity_from_mrz` auf gleichem Salt; Pass bare+10er-Form). Bewusste Grenze: API-Caller, die PII am Pre-Send-Scan vorbeischicken, werden vom Tool-Seam nicht mehr nachdetektiert (Interactive-Clients scannen server-seitig; `gdpr_pick_model_for_background` scannt unverändert selbst).

**Nachtrag 9.383.1 (Chat db0ef544):** dob-Spans tragen den Keyword-Prefix ('geboren am 05.02.1947') und waren NUR als Voll-Span-Paar registriert — formatiert das Modell die Antwort um (Markdown-Fett, anderes Format), reißt der exakte Reverse-String und das Fake-Datum bleibt sichtbar (VORBESTEHENDE Schwäche, nicht 9.383.0-verursacht). Fix: `_entity_fake_dob` in `_ENTITY_GENERATORS` (dob+date) registriert zusätzlich das nackte Datum in allen `_dob_surface_forms` (wie der MRZ-Seed); `purge_value`/`values_same_subject` extrahieren Datum auch aus Prefix-Spans. Merksatz: **Shape-Fakes mit Kontext-Prefix brauchen IMMER auch das nackte Wert-Paar, sonst bricht der Reverse bei Umformatierung.**

**Nachtrag 9.383.3 (Chat 80494e34, dritte Schicht):** Tabelle zeigte Fakes AUCH NACH RELOAD — `metadata.text_rounds` (Per-Runden-Segmente) kommt ROH aus `_result['text_segments']` und lief nie durch die Rückübersetzung; bei Multi-Round-Turns rekonstruiert der Reload BEVORZUGT aus text_rounds statt aus dem restaurierten content (sessions.js). Fix: `_deanonymize_text_rounds` an der Persist-Stelle; 3 Bestandsnachrichten offline über ihre persistierten Mappings repariert. Merksatz: **JEDER persistierte Text-Kanal einer Antwort braucht den Deanonymize-Pass — content allein reicht nicht, die UI rendert bei Tool-Turns aus text_rounds.**

**Nachtrag 9.383.2 (Chat 80494e34):** Tabellen-Zellen zeigten LIVE Fake-E-Mail/-IBAN/-Datum, obwohl die persistierte Antwort korrekt war — `StreamingDeanonymizer` hielt nur bei unvollständigen `<`-Opaque-Tokens zurück; SHAPE-Fakes streamen in kleinen Deltas, das Fake-Präfix wurde irreversibel emittiert (Client rendert additiv; done-Swap greift bei Tool-Turns nicht — assistant_segment behält den Stream-Text). Fix: Tail-Holdback = längster Fake in mapping.reverse +8 (Cap 384, pro Feed neu — Mapping wächst mid-turn). Merksatz: **Streaming-Rückübersetzung braucht ein Holdback in Fake-Länge, nicht nur Token-Marker; „persistiert korrekt, live falsch" ⇒ immer den text_delta-Stream selbst prüfen** (SSE: Typ steht in der `event:`-Zeile, nicht im data-Payload). OFFENES Follow-up: Tabellen splitten Fake-Namen in Zellen („Vorname: Maria / Nachname: Sam Stark") — einzelne Fake-VORNAMEN haben bewusst keine Reverse-Paare, `lint_residual_fakes` flaggt sie nicht (Linter-Lücke).

Tests: `tests/test_gdpr_decision_driven.py` (32); GDPR-Familie 287 grün. Live: 3-Turn-Matrix (FP-DOB klar / nachträgliches FP → Purge + Klartext / ledger-only sticky hält; Pass-Fake über alle Turns stabil) + Fett-Datum-Reverse. Vorbestehender Ordnungs-Flake dokumentiert: test_l7_cleanup_fixes VOR test_gdpr_mapping_every_turn kippt test_web_gate_inactive_without_mapping (NER-_NLP_CACHE-Klasse wie 9.342, isoliert grün, auf HEAD identisch).

**Nachtrag 9.383.4-9.383.9 (Chat 80494e34 + systematisches Text-Audit):** eine Kette gestaffelter Text-PII-Bugs, alle live gefunden — NICHT durch Einzel-Meldungen, sondern durch ein ADVERSARIALES END-TO-END-HARNESS (scratchpad/anon_text_audit.py: scan-text→decision→chat→wire, 23 Fälle, prüft LEAK=Original im Wire / MISS=Scanner-Lücke). Ergebnisse: (9.383.4) EN-NER-Modell taggt deutsche Feld-LABELS ('Deutsche Steuer-ID'→'Jordan Davis-ID') als PERSON → _is_field_label_span + Fenster-Sprachcheck für untrusted. (9.383.5) Datenschutzbericht 49 Zeilen = 34 abgeleitete Varianten → Mapping.derived + is_derived-Spalte, nur ANZEIGE filtern; deutsche Labels via gdprRuleLabel. (9.383.6) gesplittete Fake-Vornamen ('Vorname: Maria') → distinktive Vornamen-Paare (≥4) + Linter name_given_lone + deanonymize_text word-boundet rein-alphabetische Keys (sonst 'Cameron'→'Cameronstrasse'). (9.383.7) nationale IDs opak → _fake_national_id: formtreu + prüfziffer-gültig via REJECT-SAMPLING gegen die echten pii_ner-Validatoren (Budget 2000; opportunistisch für ctx-Regeln). (9.383.8) Telefon nur +49 / Ortsnamen nie → phone-Regex 3. Zweig (Slash/Klammern/2-Gruppen), Address-Gate: Stadt nach PLZ + Wohnbezug/Person+lokativ (mit Bewegungsverb-Ausschluss). (9.383.9) Namens-Genitiv 'Bonnie Starks' → Post-Pass + Stamm-Seeding + s-erhaltende Faker (Apostroph bei s/x/z/ß). MERKSÄTZE: **Text ist die BASIS — mit Attachments/Projektwissen steigt nur die Komplexität; systematisches Audit-Harness > auf Einzelmeldungen warten. Wire-Wahrheit = metadata.wire_content prüfen, nie content. Zwei unabhängige Address-Gates: _passes_address_precision_gate (Span-lokal) UND der Person-Proximity-Gate in _pii_scan_text (braucht name_spans) — beide müssen passieren.** EHRLICHE GRENZEN (dokumentiert, nicht gelöst): isolierter Namens-Genitiv ohne getaggten Basisnamen (spaCy-Recall), invertierte Namen 'Stark, Bonnie' teils.

Verwandt: [[feedback_gdpr_rule_action_is_the_enforcement]] · [[project_gdpr_decision_ledger_wire]] · [[feedback_single_fix_point]] · [[project_pii_parity_wave2_m6_m9]] (MRZ-Seed-Historie 9.339.0)
